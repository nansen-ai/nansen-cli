name: Sync Skills to ClawHub

on:
  workflow_call:
    secrets:
      CLAWHUB_TOKEN:
        required: true
  workflow_dispatch: # manual trigger if ever needed

jobs:
  sync-clawhub:
    # Covers both workflow_call and manual workflow_dispatch on this revision.
    if: vars.CLI_PUBLICATION_ENABLED == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 22
          cache: 'npm'

      - run: npm ci

      - name: Login to ClawHub
        run: npx clawhub login --token "$CLAWHUB_TOKEN" --no-browser
        env:
          CLAWHUB_TOKEN: ${{ secrets.CLAWHUB_TOKEN }}

      - name: Sync skills to ClawHub
        run: npx clawhub sync --all --workdir . --dir skills
