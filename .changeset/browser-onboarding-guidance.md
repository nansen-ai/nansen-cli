---
"nansen-cli": patch
---

Use the shared offline credential resolver during installation, offer an explicit free account check, and align help, schema and research skills with browser-session scope. Preserve key-only workflows, native storage requirements and the major login/payment migration. Record outstanding release gates and require a tested v2-compatible recovery binary. Preserve configured skill API-key injection, require selected credentials while preserving automatic renewal for authorized research, and run postinstall correctly through symlinked paths.
