---
"nansen-cli": minor
---

Add `--format markdown` output format — zero-dependency GFM rendering for agent pipelines. Arrays render as GitHub-flavoured Markdown tables; single objects as bold key-value lists. LLMs parse markdown natively, making it ideal for piping output directly into prompts or agent context.
