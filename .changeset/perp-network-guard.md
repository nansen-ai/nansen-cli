---
"nansen-cli": patch
---

`perp` commands refuse to run when `NANSEN_HL_API_URL` points at the Hyperliquid testnet, instead of signing mainnet asset ids into testnet actions and showing mainnet positions for a testnet account
