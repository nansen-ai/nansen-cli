---
"nansen": minor
---

Add X (Twitter) post analytics commands under `nansen research x`

- `posts-by-token` — search posts mentioning a token by name or symbol
- `posts-by-user` — get posts from a specific X username

Supports `--min-likes`, `--min-views`, `--days`, `--sort`, and pagination options.
